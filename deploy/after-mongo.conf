[Unit]
# Кладётся в /etc/systemd/system/<сервис>.service.d/after-mongo.conf
# База теперь на этом же сервере: без этого после перезагрузки сервис
# стартует раньше mongod и падает на первом же запросе.
After=mongod.service
Wants=mongod.service
