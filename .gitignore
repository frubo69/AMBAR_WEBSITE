.env
.DS_Store
__pycache__/
*.pyc
orders.json
user_state.json
bans.json
addresses.json
stock.json
support_messages.json
support_map.json
uploads/
.claude/
*.bak

# local design-preview copy generated by tools/preview_build.py (served at / for screenshots)
/index.html


# broadcast resume log (per-VPS runtime state)
.broadcast_sent_*.txt

# Секреты и рабочее состояние сервера. Лежат внутри рабочего каталога, а он же
# — репозиторий: без этих строк ключ от хранилища копий и пароль их шифрования
# однажды уедут в гит вместе с обычным коммитом.
.env.bak-*
.b2.env
.b2-pass
.b2-last
.watchdog-state.json
backups/

# отчёты проверки безопасности: их место на диске, а не в репозитории
strix_runs/
